#!/bin/sh

systemctl is-active --quiet tailscaled || exit 1
ip link show tailscale0 2>/dev/null | grep -q 'UP' || exit 1
nft list table ip tailscale_gateway >/dev/null 2>&1 || exit 1
tailscale status --json 2>/dev/null | jq -e '.BackendState == "Running" and .Self.Online == true' >/dev/null
